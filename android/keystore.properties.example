# Copy this file to android/keystore.properties for release builds.
# Never commit keystore.properties — it holds signing credentials.
storeFile=/absolute/path/to/android.keystore
storePassword=replace-with-store-password
keyAlias=replace-with-key-alias
keyPassword=replace-with-key-password
